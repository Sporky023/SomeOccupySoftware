%h1 Listing tags

%table
  %tr
    %th
    %th
    %th

  - @tags.each do |tag|
    %tr
      %td= link_to tag.name, :controller => "tags", :action => "show", :id => tag
%br

= link_to 'New Tag', new_tag_path
